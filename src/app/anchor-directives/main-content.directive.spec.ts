import { MainContentDirective } from './main-content.directive';

describe('MainContentDirective', () => {
  it('should create an instance', () => {
    const directive = new MainContentDirective();
    expect(directive).toBeTruthy();
  });
});
